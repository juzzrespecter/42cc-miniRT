cylinder objects // inside illumination still doesnt fully work --> check sup normals when camera axis and cy axis are aligned
every object // far enough, lightning stops working properly (tmax ??)
clean exits // check segfaults when rt stops with an error
